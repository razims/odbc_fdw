services:
  dev:
    build:
      context: .
      target: dev
    working_dir: /workspace
    volumes:
      - .:/workspace
    stdin_open: true
    tty: true

  test:
    build:
      context: .
      target: test-runner
    working_dir: /workspace
    command: ["bash", "/workspace/docker/run-local-tests.sh"]

  hana:
    build:
      context: .
      target: test-runner
    working_dir: /workspace
    env_file:
      - .env
    command: ["bash", "/workspace/docker/probe-hana.sh"]
